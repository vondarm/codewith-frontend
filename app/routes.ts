export const ROUTES = {
    HOME: '/',
    WORKSPACE: '/workspace',
    CREATE_WORKSPACE: '/create_workspace',
    ROOM: '/room',
    LOGIN: '/login',
    REGISTER: '/register',
    getWorkspaceRoute: (id: number) => `/workspace/${id}`,
}
